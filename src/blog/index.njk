---
layout: page
title: آخرین نوشته‌های وبلاگ
eleventyExcludeFromCollections: true
---
        {% for post in collections.blog %}
    <h2 style="display:inline;font-size:1em"><a href="{{ post.url }}">{{ post.data.title }}</a></h2><span style="font-size:0.9em;font-style:italic">&nbsp;[{{ post.date | toPersianDate }}]</span> </em><br>
        {% endfor %}
